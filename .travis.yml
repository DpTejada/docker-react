sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t DpTejada/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true DpTejada/docker-react npm run test